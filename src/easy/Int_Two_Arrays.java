package easy;

public class Int_Two_Arrays {

    public int[] intersect(int[] nums1, int[] nums2) {
        int[] bigger;
        int[] smaller;
        if (nums1.length >= nums2.length) {
            bigger = nums1;
            smaller = nums2;
        } else {
            bigger = nums2;
            smaller = nums1;
        }

        int count = 0;
        int bigIndex = 0;

        for (int i = 0; i < smaller.length; i++) {

        }

        return null;
    }
}
